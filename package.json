{
  "name": "@ndml/types",
  "version": "1.0.4",
  "description": "Types for ndml",
  "main": "lib/index.js",
  "author": "LIMPIX31",
  "license": "MIT",
  "types": "./lib/types/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/NodiumMC/ndml-types.git"
  },
  "homepage": "https://github.com/NodiumMC/ndml-types",
  "bugs": {
    "url": "https://github.com/NodiumMC/ndml-types/issues"
  },
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "build": "tsc && esbuild index.ts --bundle --minify --platform=node --external:./node_modules/* --format=cjs --outfile=./lib/index.js",
    "prepare": "yarn build",
    "version": "yarn format && git add -A",
    "postversion": "git push && git push --tags",
    "format": "prettier --write \"*.ts\""
  },
  "exports": "./lib/index.js",
  "devDependencies": {
    "esbuild": "^0.14.38",
    "prettier": "^2.6.2",
    "typescript": "^4.6.3",
    "yarn": "^1.22.18"
  }
}
